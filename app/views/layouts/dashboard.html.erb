<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <title>Beacon - <%= content_for(:title) || "Uptime Monitoring" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= action_cable_meta_tag %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full antialiased" style="font-family: 'Inter', system-ui, sans-serif; background: #FAF9F7;">
  <div class="min-h-full">
    <!-- Header -->
    <header class="border-b" style="border-color: #E8E5E0; background: #FFFFFE;">
      <div class="mx-auto max-w-5xl px-6">
        <div class="flex h-16 items-center justify-between">
          <%= link_to dashboard_root_path, class: "flex items-center gap-2.5" do %>
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <circle cx="10" cy="10" r="8" fill="#10B981"/>
              <path d="M7 10L9 12L13 8" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span class="text-[15px] font-semibold tracking-tight" style="color: #1A1A1A;">Beacon</span>
          <% end %>

          <% if @project&.persisted? %>
            <div class="flex items-center gap-6">
              <div class="flex items-center gap-3">
                <span style="color: #C4C0B8;">/</span>
                <%= link_to @project.name || "Project", dashboard_project_overview_path(@project), class: "text-[14px] hover:underline", style: "color: #6B6760;" %>
              </div>
              <nav class="flex items-center gap-4 text-[13px]">
                <%= link_to "Overview", dashboard_project_overview_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'overview' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Monitors", dashboard_project_monitors_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'monitors' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Incidents", dashboard_project_incidents_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'incidents' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Status Pages", dashboard_project_status_pages_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'status_pages' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Maintenance", dashboard_project_maintenance_windows_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'maintenance_windows' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Setup", dashboard_project_setup_path(@project), class: "font-medium transition-colors", style: "color: #{controller_name == 'setup' ? '#1A1A1A' : '#8B8780'};" %>
              </nav>
              <%= link_to dashboard_projects_path, class: "text-[13px] font-medium px-3 py-1.5 rounded-lg transition-colors hover:opacity-80", style: "background: #F0EFED; color: #6B6760;" do %>
                Projects
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </header>

    <!-- Main -->
    <main class="mx-auto max-w-5xl px-6 py-10">
      <% if notice %>
        <div class="mb-6 px-4 py-3 rounded-lg text-[14px]" style="background: #E8F5E8; color: #2D5A2D;">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="mb-6 px-4 py-3 rounded-lg text-[14px]" style="background: #FEE8E8; color: #8B2D2D;">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </div>
</body>
</html>
