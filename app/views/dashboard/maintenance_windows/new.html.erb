<% content_for(:title) { "Schedule Maintenance" } %>

<div class="max-w-2xl mx-auto space-y-6">
  <div>
    <h1 class="text-[22px] font-semibold tracking-tight" style="color: #1A1A1A;">Schedule Maintenance</h1>
    <p class="mt-1 text-[13px]" style="color: #8B8780;">Schedule a maintenance window to suppress alerts and notify subscribers</p>
  </div>

  <%= render "form", maintenance_window: @maintenance_window %>
</div>
