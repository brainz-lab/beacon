<% content_for(:title) { "Edit Maintenance" } %>

<div class="max-w-2xl mx-auto space-y-6">
  <div>
    <h1 class="text-[22px] font-semibold tracking-tight" style="color: #1A1A1A;">Edit Maintenance Window</h1>
    <p class="mt-1 text-[13px]" style="color: #8B8780;">Update maintenance window details</p>
  </div>

  <%= render "form", maintenance_window: @maintenance_window %>
</div>
